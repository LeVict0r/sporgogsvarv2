# Q&A Lite – Streamlit (Single-file)

Denne version er én fil: `app.py`. Brug den som Main file path på Streamlit Cloud.

## Lokal kørsel
```bash
pip install -r requirements.txt
streamlit run app.py
```
